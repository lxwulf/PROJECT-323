---

- name: remove unused packages
  package:
    name: python-unversioned-command
    state: absent
  become: yes

- name: installing standard packages
  package:
    name: "{{ packages }}"
    state: present
  become: yes
